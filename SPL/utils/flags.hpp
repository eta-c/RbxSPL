/*
*	@Desc: Flags for RBX utils
*	@Note: Most of these flags are fast, so at compile time
* 
*	@Tykind
*/
#pragma once
#include <cstdint>

namespace flags
{
	inline constexpr auto dissa_base{ 0x140000000 };
	inline constexpr auto application_name{ "Rnet" };
}